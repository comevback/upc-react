import './TargetTitle.css';

const TargetTitle = (props) => {
    return (
        <div className="target">
            <h1>{props.dividerName}</h1>
        </div>
    );
}

export default TargetTitle;